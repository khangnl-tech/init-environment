services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dev-php-laravel
    working_dir: /var/www/html
    stdin_open: true
    tty: true
    volumes:
      - ./app:/var/www/html
    ports:
      - "8000:8000"
    command: bash

